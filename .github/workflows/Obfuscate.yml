名字: Build Obfuscate BPB Panel

on:
  push:
    分支:
      - main
  schedule:
    - cron: "0 1 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - 名字: Check out the code
        uses: actions/checkout@v4

      - 名字: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "latest"

      - 名字: Install dependencies
        run: |
          npm install -g javascript-obfuscator

      - 名字: Clone BPB workjs
        run: |
          wget -O origin.js https://raw.githubusercontent.com/bia-pain-bache/BPB-Worker-Panel/refs/heads/main/build/unobfuscated-worker.js

      - 名字: Obfuscate BPB worker js
        run: |
          javascript-obfuscator origin.js --output _worker.js \
          --compact true \
          --identifier-names-generator hexadecimal \
          --rename-globals true \
          --string-array true \
          --string-array-encoding 'base64' \
          --string-array-threshold 0.75 \
          --transform-object-keys true \
          --self-defending false \
          --simplify true

      - 名字: 提交更改
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          分支: main
          commit_message: ':arrow_up: update latest bpb panel'
          commit_author: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          push_options: '--set-upstream'
